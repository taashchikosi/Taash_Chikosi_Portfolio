# 🤝 RetrofitGPT — Session Handoff (13 June 2026)

> **This is the latest entry point. Read this first.** For deeper detail:
> `HANDOFF_NEXT_SESSION.md` (prior layered notes) → `RUN_DEPLOY.md` (the deploy
> runbook) → `evals/README.md` (the eval gate) → `docs/ARCHETYPE_AND_CALIBRATION.md`
> (the model + calibration honesty) → `RetrofitGPT_Project_Plan.md` (spec).
>
> **Build state:** all 5 agents real · capstone runs GREEN · **two-tier eval gate
> (Tier A + live Tier B) built** · **deploy-ready backend** (token cap, CORS, routing)
> · **case-study page + one-click live demo built** · **deploy runbook written**.
> **Test suite: 131 green.**
>
> **What's left:** EXECUTE the deploy on the VPS (code is ready; only Taash can run it
> on her infra). See §7 + `RUN_DEPLOY.md`.

---

## 1. 🎯 30-second summary

RetrofitGPT is an autonomous **5-agent** system: upload a building energy model
(`.idf`) + 12 months of bills → it runs **real EnergyPlus** retrofit simulations →
outputs an audit-ready decarbonisation business case (savings %, payback, NPV, tCO₂e),
verified against **ASHRAE Guideline 14** calibration, an **OWASP-LLM06** guardrail, and
**real NCC 2022 Section J** code compliance. Physics + reference data sit behind an
**MCP** boundary; the LLM only makes judgement calls, every number is deterministic.

- **Who:** Taash (Taashira Chikosi) — building-energy engineer (IESVE background).
- **Goal:** hired in 🇦🇺 **Australia** (Big-4 sustainability/energy or agentic-AI
  engineer / solution architect). All data is Australian (NABERS, NCC, NGA, AUD).
- **Why it wins:** physics-in-the-loop via MCP + a governance layer + an **evals gate
  that drove a real model-routing decision** + deploy discipline = senior hiring signal.

---

## 2. ✅ Current state — the pipeline + the proof + the shopfront

```
 upload .idf + bills
        │
        ▼
   Retriever → Modeler → [HITL] → Sim Runner → Analyzer → Reviewer → business case
   (Claude)    (DeepSeek)          EnergyPlus   (determ.)   (Claude: GL14+LLM06+NCC)
        ▲___________ route-back (cal fail→Modeler, claim fail→Analyzer, max 3) ___________│
```

| Layer | State |
|-------|-------|
| 5 real agents + capstone E2E (`scripts/verify_pipeline.py`) | ✅ GREEN |
| **Tier A evals** (5 golden archetypes, 71 checks, mutation-tested) | ✅ |
| **Tier B evals** (live model-swap gate incl. citation judgement) | ✅ |
| **Deploy-safe backend** (health, rate-limit, token cap, CORS, eval-gated routing) | ✅ |
| **Case-study page** `/retrofitgpt` + About-model panel + demo badge | ✅ |
| **One-click live demo** (SSE trace + auto-approve + business case) | ✅ |
| **Deploy runbook** `RUN_DEPLOY.md` | ✅ |
| **Actual VPS deploy executed** | ❌ Taash's move (§7) |

**Test suite: 131 green** — `python3 -m pytest` (no Docker; uses fakes).

---

## 3. 🆕 What changed THIS session (13 Jun 2026)

1. **Verified the NCC Section J work was real** — re-checked every Table J7D3a seed value
   against the live ABCB NCC 2022 text (all match: office ≥200 lx → 4.5 W/m², etc.);
   confirmed it's wired through the MCP tool, not hardcoded. Lesson reinforced: "done &
   verified" was overstated — it was Tier-1 (per-space). So…
2. **Closed the NCC gap** — added the real **aggregate area-weighted J7D3(2)(a)** check
   (`check_aggregate_lighting_compliance`: Σ area×design ≤ Σ area×max). Honest framing
   now: per-space + aggregate done; J7D3b/c adjustment factors + J4 fabric = Tier 2.
3. **Built the evals harness — Tier A** (`evals/run_evals.py`, `evals/test_cases/*.json`):
   golden buildings → expected savings/payback/NPV/carbon **bands** + GL14 + NCC verdicts,
   over the REAL `analyse()`/`review()`/NCC functions with canned sim fixtures. Exit-coded,
   reports to `evals/results/`, **mutation-tested**, folded into pytest. A **negative
   control** (`uncalibrated_office_must_fail`) proves the GL14 gate still rejects an
   uncalibrated model. 5 archetypes: small/medium office, retail, school.
4. **Built Tier B** (`evals/tier_b.py`, `--live`): gates the DeepSeek↔Claude swap by
   running the real LLM-judgement functions against the provider. Checks classify ∈
   allowed, select ⊆ catalog + must-include, **`llm_parsed`** (model emitted valid JSON,
   not the silent fallback), **stability** across samples, and a **citation judgement**
   (`verification.guardrails.judge_claims_grounded`: clean→supported, fabricated
   rebate→unsupported, fail-closed). Needs only a provider key — **no EnergyPlus/Docker**.
5. **Ran the live swap → made a decision (see §4).** The eval caught a real DeepSeek
   regression; routing now reflects it.
6. **Reconciled the router deploy-default** — `retrieval`→Claude (was DeepSeek),
   `verification`→Claude, `modelling`→DeepSeek (`router/model_router.py`).
7. **Deploy-safety guards:** hard **token/cost cap** (rolling 24h budget in
   `model_router.complete()`, raises before spend; `/api/runs`→503 when spent; `/health`
   reports it) + **locked CORS** (`ALLOWED_ORIGINS` allowlist, no more `*`).
8. **Built the recruiter shopfront:** `/retrofitgpt` case-study page (hero + live
   `/health` dot, architecture, eval metrics, ADR, **About-the-model panel + demo badge**)
   and rewrote `app/page.tsx` into a **one-click live demo** (POST run → live SSE
   5-agent trace → auto-approve HITL → business case + verdict + badge).
9. **Deploy plumbing fixes:** added `demo_calibrate` flag + `GET /api/runs/{id}/result`
   to the API; **fixed the serve command** — compose now runs `uvicorn api.main:app`
   (the default CMD only started the MCP server, so the API was never actually served).
10. **Wrote `RUN_DEPLOY.md`** — full VPS→Caddy→Vercel runbook with troubleshooting.

---

## 4. 🔑 The model-swap decision (eval-gated, 13 Jun 2026)

Live Tier B, real providers, 3 samples/case:

| Node (router task) | Claude (sonnet-4-6) | DeepSeek (deepseek-chat) | Verdict |
|---|---|---|---|
| Retriever classify (`retrieval`) | 4/4 ✅ | ❌ stably mis-sized a 511 m² office as "medium"; a prompt fix corrected offices but then destabilised retail + mislabelled the school | **Claude** |
| Modeler select (`modelling`) | ✅ | ✅ | DeepSeek |
| Citation judge (`verification`) | ✅ | ✅ caught all fabricated rebates | DeepSeek OK, but gate runs on Claude in deploy default |

Cost (full Tier-B run): Claude **8335/2302** tok · DeepSeek **7440/1592** tok.
**Lesson:** prompt-engineering around a weaker model on a low-stakes node is whack-a-mole
— let the eval pick the model per node. (Kept the explicit office-size-threshold prompt
in `agents/retriever.py::_CLASSIFY_SYSTEM` — Claude-safe + more correct; guard test in
`tests/test_retriever.py`.)

---

## 5. 🗂️ File map (🆕 new / ✏️ changed this session)

```
retrofitgpt/
├── evals/                              🆕 THE EVAL GATE
│   ├── run_evals.py                    🆕 Tier A runner + --live (Tier B) + reports
│   ├── tier_b.py                       🆕 live LLM-judgement gate (classify/select/citation/stability)
│   ├── README.md                       🆕 how it works, Tier A vs B, how to gate a swap
│   └── test_cases/*.json               🆕 5 golden buildings (+ negative control)
├── verification/
│   ├── ncc_compliance.py               ✏️ + aggregate area-weighted J7D3(2)(a) check
│   └── guardrails.py                   ✏️ + judge_claims_grounded (LLM citation review)
├── router/model_router.py              ✏️ retrieval→Claude default + 24h token budget cap
├── agents/retriever.py                 ✏️ _CLASSIFY_SYSTEM anchors office size thresholds
├── api/main.py                         ✏️ token-budget 503 gate · CORS allowlist · demo_calibrate · /result
├── frontend/
│   ├── app/page.tsx                    ✏️ one-click live demo (SSE trace + auto-approve + result)
│   ├── app/retrofitgpt/page.tsx        🆕 case-study page + About-model panel + demo badge
│   └── .env.local.example              🆕 NEXT_PUBLIC_API_BASE template
├── docker-compose.yml                  ✏️ app now runs `uvicorn api.main:app` (was MCP-only)
├── tests/                              ✏️ +test_evals, +test_evals_tierB, +test_deploy_guards,
│                                          +test_auto_run, test_ncc/retriever/model_router updated
├── RUN_DEPLOY.md                       🆕 VPS → Caddy → Vercel runbook
└── HANDOFF_2026-06-13.md               🆕 this file
```

---

## 6. 🧪 How to run / verify

```bash
cd "/Users/taashchikosi/Documents/Claude/Projects/Agentic AI Portfolio/retrofitgpt"

# unit + eval suite (no Docker) — expect 131 passed
python3 -m pytest

# Tier A eval gate (deterministic, no key) — expect ALL GREEN, exit 0
python3 evals/run_evals.py

# Tier B live model-swap gate (needs a provider key; runs without Docker)
LLM_PROVIDER=anthropic python3 evals/run_evals.py --live --samples 3
LLM_PROVIDER=deepseek  python3 evals/run_evals.py --live --samples 3   # compare

# capstone E2E (Docker; EnergyPlus + Claude) — the green demo path
docker compose run --rm app python scripts/verify_pipeline.py --calibrate-demo
```

---

## 7. 📍 NEXT — execute the deploy (highest leverage)

The build is done; the remaining work is **operational and on Taash's infra**. Follow
`RUN_DEPLOY.md`. Critical order + gotchas:

1. **Phase 1 first — get `/health` green locally on the VPS** before Caddy/Vercel. This
   isolates "does the container run" from "is the networking wired".
   `docker compose up -d --build` (first build ~50 min — use `tmux`), then
   `curl localhost:8080/health`.
2. **⚠️ On the VPS, leave `LLM_PROVIDER` UNSET.** The dev `.env` has `=anthropic`; that
   would route everything to Claude (expensive). Unset = the eval-gated cost-tiered default.
3. **🔗 Match the CORS pair:** `NEXT_PUBLIC_API_BASE` (Vercel) ↔ `ALLOWED_ORIGINS`
   (backend) must be the exact same origin (scheme, no trailing slash). #1 cause of a
   dead demo.
4. Caddy reverse-proxies `api.<domain>` → `:8080` (auto-TLS). Vercel root dir = `frontend`.
5. Smoke test: open the site → 🟢 dot → click **Run** → watch the 5-agent trace → green
   business case + Demo-calibration badge.

**After deploy (cadence):** 🎓 learning session on the whole system → 💼 LinkedIn post
(angle: "my eval gate caught a real model regression and I routed per-node"). Then the
remaining Phase-3 bricks: Tier-2 NCC RAG, real QS cost DB, CDR tariff API.

---

## 8. ⚠️ Known gaps / honesty caveats (be precise in interviews)

| Gap | Severity | Note |
|-----|----------|------|
| Demo uses **synthetic calibration** on a **DOE prototype**, not a real building | 🟡 | Disclosed by the About-model panel + Demo badge. Real calibration = next milestone. |
| NCC compliance is **Tier 1.5** (per-space + aggregate) | 🟡 | J7D3b/c adjustment factors + J4 fabric façade calc = Tier 2 (RAG). |
| HITL gate is **auto-approved** in the public demo | 🟢 | Deliberate (one-click); the gate is real and API-enforced. |
| Frontend **not rendered-tested** (no node_modules in the build sandbox) | 🟡 | Backend logic is tested; confirm with `npm run dev`. |
| Catalog costs + tariff are **SEED/stub** | 🟡 | Replace with QS cost DB + CDR tariff API (Phase 3). |
| Tier B classify expectations lean on `idf_meta` **hints** | 🟢 | Real Retriever classify input is thin; a robust classifier would read Zone/People objects. |

---

## 9. 🧠 How to work with Taash (her rules — follow them)

- 🎨 **Visual / systems thinker** — diagrams, tables, structured layouts, emojis. Full
  picture first, then steps. **No walls of text.** Bullets over paragraphs.
- 🗣️ **Advisor, not a yes-man.** Never open with agreement. Lead with the uncomfortable
  truth. Tag confidence `[Certain]/[Likely]/[Guessing]`. Disagree with structure
  (reason → alternative → risk). Don't fold under pushback without new information.
- 🔬 **Verify, don't assume.** Primary sources, run the code, show the proof. A green ✅
  is not a correct value — sanity-check numbers against domain knowledge.
- 📣 **Narrate before each step** — one line on *what* + *why*.
- 🚫 The assistant **cannot** run EnergyPlus / eppy / live-LLM / a browser in its sandbox
  — build with injected fakes + tests; hand Taash the command for the real proof.
- 🔐 **Never put real API keys in chat.** Keys live in `.env`; rotate any that leaked.
```
